#include<stdio.h>

int main(int argc, char const *argv[])
{
    char *str = "HOHUUDOAN"; // Khởi tạo chuỗi ký tự
    printf("String: %s\n", str); // In chuỗi ký tự
    str = "HEHE"; // Thay đổi chuỗi ký tự
    printf("Modified String: %s\n", str); // In chuỗi ký tự đã


    
    getchar(); // Đợi người dùng nhấn phím trước khi kết thúc chương trình
    return 0;
}
